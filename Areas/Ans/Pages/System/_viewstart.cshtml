@*
Компонент библиотеки Ans.Net8.Web.
Код не предназначен для редактирования и компиляции во время выполнения!
*@
@{
	Layout = "_layout";

	Current.Node.AddParent(null, "Ans");
	Current.Node.AddParent(null, "System");

	Current.Node.SetPages("", null);
}
